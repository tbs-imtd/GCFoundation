@using GCFoundation.Common.Utilities
@using GCFoundation.Components.Services
@inject UserLoginService UserLoginService

@{
    // Create user login model from the current HTTP context
    // This will automatically extract user information from claims and session
    var userLoginModel = UserLoginService.CreateViewModelFromContext();
    
    // Set the current culture for proper localization
    userLoginModel.CurrentCulture = LanguageUtility.GetCurrentApplicationLanguage();
}

@* Render the actual user login partial *@
<partial name="_UserLoginPartial" model="userLoginModel" />

@* 
This is an example of how to use the user login partial in a real application.
Simply include this partial in your layout or views where you want to display user login information.

To use this in your controller:
ViewData["LoginPartialViewName"] = "_RealWorldUserLogin";

Or directly in a view:
<partial name="_RealWorldUserLogin" />
*@

